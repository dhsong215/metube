mixin video(video)
    div 
        a(href=`/video/${video.id}`)=video.title
        ul
            li #{video.rating}/5
            li #{video.comment}comments
            li date: #{video.createdAt}
            li #{video.views}views
        